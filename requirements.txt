# KYC Verification System — Resolved requirements for Python 3.11 / Windows x64
# Install order matters — see models/README.md for insightface wheel instructions.
#
# IMPORTANT: insightface has NO official Python 3.11 wheel on PyPI.
# Install the pre-built wheel FIRST (before this file):
#   pip install https://github.com/Gourieff/Assets/raw/main/Insightface/insightface-0.7.3-cp311-cp311-win_amd64.whl
# Then downgrade onnx + numpy + protobuf to satisfy paddlepaddle:
#   pip install "onnx==1.14.1" "numpy==1.26.4" "protobuf==3.20.2"
# Then install the rest:
#   pip install -r requirements.txt

# ─── GUI ──────────────────────────────────────────────────────────────────────
customtkinter==5.2.2
CTkMessagebox==2.5
Pillow==10.1.0

# ─── Computer Vision ──────────────────────────────────────────────────────────
opencv-python==4.8.1.78

# ─── OCR ──────────────────────────────────────────────────────────────────────
# numpy<2 constraint; protobuf<=3.20.2 on Windows
paddlepaddle==2.6.2
paddleocr==2.8.1
numpy==1.26.4
protobuf==3.20.2

# ─── Face Analysis ────────────────────────────────────────────────────────────
# insightface: install via pre-built wheel (see comment at top)
onnxruntime==1.16.3
onnx==1.14.1

# ─── Deep Learning ────────────────────────────────────────────────────────────
torch==2.1.2
torchvision==0.16.2
timm==0.9.7

# ─── ML / Scoring ─────────────────────────────────────────────────────────────
xgboost==2.0.3
scikit-learn==1.3.2
scipy==1.11.4
